<%= form_for @deck do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.label :subject %>
  <%= f.text_field :subject %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit "Create New Deck", class: "btn btn-large btn-primary" %>
<% end %>
